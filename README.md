# python-utilities

a small grab bag of python utility scripts for everyday tasks.

## scripts

- `timezones.py`  
  convert a naive datetime between timezones (e.g. et → cet).
- `string_tools.py`  
  basic helpers for slugifying and trimming text.

## requirements

- python 3.9+
- `pytz` library

install:

```bash
pip install -r requirements.txt
